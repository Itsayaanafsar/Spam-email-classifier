# Spam-email-classifier
Spam Email Detection using TF-IDF and Multinomial Naive Bayes A machine learning project that classifies emails as spam or ham using NLP. The data is cleaned and vectorized with TF-IDF, and a Multinomial Naive Bayes model is trained to achieve high accuracy on a large dataset and support predictions on custom emails.
